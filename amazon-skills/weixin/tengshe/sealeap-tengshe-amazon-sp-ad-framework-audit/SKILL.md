---
name: sealeap-tengshe-amazon-sp-ad-framework-audit
description: "Audit Amazon Sponsored Products structure by objective, targeting, search-term evidence, placement, and unit economics. Use when an account has tangled campaigns, unclear keyword migration, rising ACoS, or no defensible optimization order."
---

# Amazon SP 广告框架审计

## 目标

把 SP 广告拆成发现、验证、防守和扩量任务，使每个活动有独立预算、证据和停止规则，并先解决页面与商业模型问题。

## 适用任务

- 做当前状态的只读诊断。
- 形成有证据、可审批、可回退的计划或单变量实验。
- 判断信息不足时应继续验证、暂停还是保持 `HOLD`。

## 开始前要拿到

- 站点、ASIN、广告活动/广告组/目标/搜索词/广告位报告。
- 业务报告、自然与广告订单、价格、库存、毛利和变更日志。
- 产品阶段、目标关键词、Listing 承接与允许的预算风险。

缺少字段时明确标为 `UNKNOWN` 或 `NEEDS_EVIDENCE`，不要补造数据。

## 不可妥协的边界

- Amazon 当前官方规则、账户实时状态和一方业务报告优先；公开内容与第三方数据只能作为待验证启发或代理证据。
- 数值、政策、因果与执行状态分别标为 `FACT`、`ESTIMATE`、`ASSUMPTION` 或 `UNKNOWN`。
- 不捏造销量、搜索量、成本、产品属性、合规状态、评论、广告结果或平台机制。
- 默认提供诊断或草案。写入前展示对象、旧值、新值、影响、停止线与回退，核对用户已有授权是否覆盖对象、动作与预算；范围已明确授权时继续执行并回读核验，只有未覆盖或扩大的范围才请求批准。
- 不保存或输出素材来源身份、账号、链接、文章编号、发布日期、阅读量、原始话术或其他可反查线索。
- 平台界面、功能、费率、资格和规则会变化；执行前必须在目标站点复核。

## 工作流

1. 统一时间窗、归因、父子体和货币口径，标记缺失或延迟数据。
2. 按发现、验证、防守、扩量和清理目标给现有单元重新分类。
3. 将搜索词与投放词分开，识别有稳定转化、偶然转化和持续浪费项。
4. 先排查相关性、价格、评价、页面和库存，再讨论竞价与广告位。
5. 形成搜索词迁移、否定、预算与出价草案，每次只改变一个主要杠杆。
6. 按净贡献、CVR、CPC、搜索词质量和库存复盘，达到停止线即降级。

## 判断规则

- 广告只能购买可见性，不能保证购买或自然排名。
- ACoS 必须与贡献毛利、TACoS 和产品阶段一起判断。
- 论坛经验、固定竞价和固定周期均为假设，不能替代账户数据。

## 必须交付的结果

- 广告结构与目标地图
- 搜索词证据分层
- 优化优先级草案
- 单位经济、止损和复盘表

结尾列出站点、数据窗口、口径、证据、关键假设、缺口、风险、下一步和所有待批准动作。证据不足时写 `HOLD`，不得包装成可直接执行。

执行表、指标和质量检查见 [references/playbook.md](references/playbook.md)。

