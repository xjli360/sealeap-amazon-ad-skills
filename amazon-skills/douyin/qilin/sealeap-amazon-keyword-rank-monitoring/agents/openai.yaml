interface:
  display_name: "Amazon 关键词位置监控"
  short_description: "按订单贡献监控核心词并用护栏触发诊断，依据真实证据形成可复核的判断与行动方案"
  default_prompt: "Use $sealeap-amazon-keyword-rank-monitoring to 按订单贡献监控核心词并用护栏触发诊断。先获取必要数据，区分事实、估算和假设，输出可审核方案；执行写操作前核对已有授权；未覆盖的对象、动作或预算才请求批准。"
